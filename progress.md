# Progress — malveon check v1

> Update this file as implementation continues. See `CLAUDE.md` in this folder for the full spec behind every item here.

## Status

**Phase:** All 9 checks built, tested, and proven end-to-end through the real CLI, plus 5 real gaps closed this session: wiring now names which side is missing, overlap flags a real "possibly unreachable" heuristic, contract checks request-field agreement (not just HTTP method) for JS/TS, frontend overlap risk covers plain CSS/SCSS/LESS (not just Tailwind), and Python/Go now have real multi-file fixtures proving the pipeline end-to-end (previously unit-tested in isolation only). Zero manual/self-report steps left anywhere in the tool. `beta-test/` is its own real git repo, release binaries built for 4 platforms (not yet rebuilt with this session's changes — needed before the next release). Waiting on the founder's go-ahead before anything goes public (push + GitHub Release).
**Last updated:** 2026-09-16

## Locked decisions (quick reference)

| Decision | Answer |
|---|---|
| Language | Go |
| Parser | Own, pure Go — no cgo, no tree-sitter, graphify used as a design reference only |
| v1 language coverage | Python, TypeScript, JavaScript, Go |
| Checks in v1 | Wiring, Contract match, Overlap, Frontend-overlap-risk, Not-in-plan flag, Hero-act (automatic only — manual removed 2026-09-16), Incompleteness, Confidence (9 total) — **all 9 built** |
| Plan input formats | JSON (`.json`), Markdown checklist (`.md`), plain text (anything else) — all normalized to the same internal `Feature` list |
| Contract match v1 scope | HTTP method agreement only — full request/response body-shape comparison is later work, not built |
| Overlap v1 scope | Same-method+same-path route collisions only (via `graph.PathsMatch`, wildcard-aware); reachability is a best-effort heuristic added 2026-09-16 (see below), not real call-graph analysis |
| Overlap reachability heuristic | Added 2026-09-16. Each extractor now tracks which named function (if any) contains a route registration's line — Go free via `go/ast`, JS/TS via brace-matching, Python via indentation (`internal/extractor`'s shared `funcRange`/`enclosingFuncFor`). `internal/checks/overlap/reachability.go` flags a colliding node "possibly unreachable" when its enclosing function's name is never mentioned anywhere else in the codebase (word-boundary text count == 1). Anonymous functions and top-level registrations aren't judged. Any scan failure defaults to "assume referenced" — never turn uncertainty into a false claim. |
| Frontend overlap v1 scope | Tailwind utility classes ("Tailwind first," confirmed 2026-09-16) plus plain CSS/SCSS/LESS `position:` declarations (added 2026-09-16) — flags `absolute`/`fixed` with no `relative`/`sticky` anywhere in the same file. Plain-CSS context detection deliberately scans the whole file as flat text rather than brace-pairing each declaration to its selector, to avoid missing a parent selector's context due to nesting. Structural risk only, never a claim of confirmed visual overlap. Full cascade/specificity resolution and live-render confirmation both still explicitly deferred. |
| Contract request-field agreement | Added 2026-09-16. JS/TS only: does the call send every field the route handler reads off `req.body`? Both sides must resolve to a plain literal object (no spread, no variable) — a spread anywhere marks the whole object unresolved rather than guessing. Reported as its own `BodyVerdict`, separate from the method-agreement `Verdict`. Python/Go and response-shape comparison still explicitly deferred. |
| Wiring missing-side reason | Added 2026-09-16. `NOT TESTED` now names exactly which side is missing (backend-only / frontend-only / neither) instead of one generic "frontend and/or backend" message. |
| Python/Go fixture parity | Added 2026-09-16. `testdata/fixture-python` and `testdata/fixture-go` prove wiring + contract end-to-end for both languages, the same four verdict shapes (PASS/FAIL/NOT TESTED/wired-but-method-mismatched) already proven for JS/TS — previously these languages were only unit-tested in isolation (`extractor_test.go`). |
| Confidence check | Claim source is automatic by default — reads commit messages since session start (`gitutil.CommitMessagesSince`), no manual prompt needed. `--claimed-summary` still works as an explicit override. Cross-references whatever claim it finds against the wiring verdict — doesn't parse confidence language as evidence on its own |
| Hero-act automatic sourcing | Commit-message sourcing considered and rejected (circular — see below). Built the real alternative instead: `malveon watch` (`internal/watch`, `fsnotify`) captures debounced file snapshots independent of git; `internal/checks/heropatterns` checks captured history for a small, named catalog of known bug patterns (JS/TS assignment-in-condition, Go empty-error-handling, Python bare-except) that appeared in an earlier snapshot and are gone from the current version. Zero self-report, zero commit dependency. |
| Hero-act manual path | **Removed 2026-09-16.** The old `--bugs-reported`/`internal/checks/heroact` package (file-level, self-reported) is deleted outright — the automatic path covers the same need without trusting a hand-typed claim. |
| Hero-act scope correction | **2026-09-16 (second pass).** Founder corrected the framing: "find bugs created in the session," not "find bugs created *and* fixed." Added a second, independent signal — `gitutil.FileAtRef` compares each session-changed file's content at session start against its current version, always available once a session started, no watcher needed. Flags a pattern present now and absent at session start as `INTRODUCED THIS SESSION — STILL PRESENT`. The original watch-based signal is renamed `INTRODUCED & FIXED SAME SESSION` and kept as the only way to see a pattern that appeared and disappeared entirely within the session. Confirmed via `/software`'s `evolutionary-architectures` routing that these are one fitness function at two cadences, not two separate checks. |
| Incompleteness check | **Added 2026-09-16.** `internal/checks/incompleteness` scans files changed since session start for `TODO`/`FIXME`/`HACK`/`XXX`/"not implemented" markers. Code-only, one-directional (presence is proof, absence isn't) — built as a separate, narrower check rather than altering `confidence.go`, since "did the agent claim X" is a linguistic question that can't be reduced to code structure the way hero-act's could. |
| Session-start snapshot | Automatic (`malveon session start` captures `git rev-parse HEAD`) |
| Deferred (not in v1) | Recurring-failure memory, commit-blocking exit code, plain-CSS cascade resolution, live-render overlap confirmation |
| First real tester | Feeling_Sun_6436 (Reddit) — committed to running it against their own real repo |
| Distribution | Prebuilt binaries via GitHub Release (chosen over "clone and build," which needs Go installed on the tester's end) |

**Architecture correction (2026-09-11):** dropped `go-tree-sitter` (cgo) for a pure-Go extractor after finding no C compiler on the build machine — also the better call for binary portability generally. See `CLAUDE.md` section 5.

**Repo-structure fix (2026-09-16):** `beta-test/` was nested inside the private `D:\Customer talk` workspace repo, and that outer repo's `origin` remote was pointed at the *public* `github.com/LadsonDavid/beta-test.git` — one `git push` away from leaking all the private Reddit research and session notes. Fixed: `beta-test/` is now its own real git repo (own `.git`, one clean commit, only actual product files) with that remote; the outer private repo now has no remote at all. Nothing had been pushed before this was caught.

## What's actually built and tested right now

- **`internal/graph`** — Node/Graph model, `FindNodesMatching`, `PathsMatch` (`:id`/`{id}` wildcards), `MatchedPairs`. Unit tested.
- **`internal/extractor`** — pure-Go, all 4 languages (JS/TS route+call extraction incl. `fetch` method resolution, Python decorators/`requests.*`, Go via real `go/ast`). Never rounds a dynamic path up to a literal.
- **`internal/features`** — multi-format plan loader (JSON/Markdown/plain-text), one unchanged external signature (`Load(path)`) regardless of format — routed through `philosophy-of-software-design` to keep it a deep module instead of leaking format concepts to callers. Slug-based ID generation with de-dup. Tested against all 3 formats.
- **`internal/gitutil`** — shells out to system `git`. Tested against real temp repos.
- **`internal/session`** — `Start`/`Load` for `.malveon/session.json`. Tested.
- **`internal/watch`** — background file-watcher (`fsnotify`, first external dependency), continual cadence, debounced snapshots to `.malveon/history/`, own liveness heartbeat/status for honest crash detection. Tested with real fsnotify events against a real temp dir (not mocked) — captures correct content per generation, sequential seq numbers, correct running→stopped status transitions.
- **`internal/checks/heropatterns`** — two independent signals, one check (2026-09-16). Git baseline (`gitutil.FileAtRef`, always available once a session started) flags a known pattern present now and absent at session start as `STILL PRESENT`; `internal/watch` history flags one present in an earlier snapshot and absent now as `FIXED SAME SESSION`. 3-pattern catalog (JS/TS assignment-in-condition, Go empty-error-handling, Python bare-except). Tested end-to-end: a real watch session (introduce bug → fix it → verify detection), the git-only still-present case (no watcher at all), a pre-existing-bug-not-flagged case, and the unavailable-without-either-signal case.
- **`internal/checks/wiring`** — PASS/FAIL/NOT TESTED, with a specific missing-side reason (2026-09-16). Proven end-to-end.
- **`internal/checks/contract`** — CONTRACT MATCH/MISMATCH/NOT TESTED for HTTP method, plus a separate `BodyVerdict` for JS/TS request-field agreement (2026-09-16, `internal/extractor/bodyshape.go`). Proven end-to-end.
- **`internal/checks/overlap`** — groups `Extracted`, known-method routes by `graph.PathsMatch` equality; flags groups >1. Scoped via `refactoring` skill guidance to exclude same-path/different-method pairs and unresolved-method pairs from ever being compared. Now also flags a "possibly unreachable" note per node (2026-09-16, `reachability.go`) when its enclosing function is never referenced elsewhere. Tested with 4 scenario pairs (true literal collision, wildcard-vs-literal collision, different-method non-collision, unresolved-method non-collision, dynamic-path non-collision) plus the reachability heuristic — all correct.
- **`internal/checks/planauthority`** — NOT IN PLAN findings, scoped to session diff. Tested against a real temp git repo.
- **`internal/checks/incompleteness`** — new (2026-09-16). Scans files changed since session start for `TODO`/`FIXME`/`HACK`/`XXX`/"not implemented" markers across `.js/.jsx/.ts/.tsx/.py/.go`. Tested: markers in session-changed files are flagged, markers predating the session are not, unavailable without a session. Proven end-to-end through the real CLI.
- **`internal/checks/confidence`** — new. Cross-references a plain-text agent summary against wiring verdicts per feature; CONFIRMED/CONFIDENCE MISMATCH/NOT TESTED. Tested for all three verdicts, including a real "claims it's fully functional but wiring says NOT TESTED" mismatch case.
- **`internal/report`** — all 9 sections rendered.
- **`cmd/malveon`** — `malveon session start [--root]`, `malveon watch [--root]`, and `malveon check [--features <path>] [--root] [--claimed-summary <path>]`.

**Full end-to-end proof, this session:** a duplicate `/refund` POST registration correctly flagged by overlap; a false "cancel order is done and working perfectly" claim (against a feature with no wiring at all) correctly flagged as CONFIDENCE MISMATCH; the plan given as a `.md` checklist instead of JSON, parsed and matched correctly through the whole pipeline.

## Build checklist

- [x] `extractor`, `graph` — pure-Go, all 4 languages, tested
- [x] `features` — multi-format plan loader (JSON/Markdown/text), tested
- [x] `wiring`, `contract`, `overlap`, `planauthority`, `heropatterns` (hero-act, automatic-only), `incompleteness`, `confidence` — all 9 checks, all proven end-to-end
- [x] `session` lifecycle
- [x] `watch` — background fsnotify-based capture, own liveness tracking, tested with real file events
- [x] `report`, `cli` — all 9 sections wired, `session start`/`watch`/`check` all run for real
- [x] Repo split — `beta-test/` is its own git repo, correct remote, private workspace repo's leak risk closed
- [x] Windows icon embedded (`rsrc_windows_amd64.syso`), verified genuinely present via .NET, verified it doesn't affect non-Windows builds
- [x] README.md written (public-facing entry point, distinct from `CLAUDE.md`)
- [ ] **Rebuild release binaries** — the 4 existing `dist/` binaries predate `malveon watch`, the hero-act manual-path removal, and the new `incompleteness` check; need a fresh build before anything ships
- [ ] **Push to the public remote** — held pending explicit founder go-ahead (public, hard-to-reverse action)
- [ ] **Create the GitHub Release** with the rebuilt binaries attached — same, held pending go-ahead
- [ ] Python/Go fixture repos — still unit-tested in isolation only, not proven end-to-end through a full multi-file fixture the way JS/TS is
- [ ] Message drafted to Feeling_Sun_6436

## Known, stated limitations (not gaps hiding as bugs)

- **Contract's request-field agreement is JS/TS only**, and only when both sides are a literal object — Python/Go and response-shape comparison aren't covered yet.
- **Hero-act only catches its 3-pattern catalog** — not a general "was this a real bug" judgment (not resolvable from static snapshots alone), and requires `malveon watch` to have actually been running without a gap. No manual fallback anymore — removed 2026-09-16.
- **Incompleteness is one-directional** — a marker's presence is real proof, its absence proves nothing (most finished code has none either). Never a substitute for the confidence check.
- **Overlap's reachability note is a best-effort heuristic, not real call-graph analysis** — an anonymous handler or a genuinely dead route it can't attribute to a named function still just counts as a plain registration, same as before 2026-09-16.
- **Frontend overlap risk (both Tailwind and plain CSS) is file-scoped, not ancestor-precise** — no real JSX tag-tree or CSS selector-specificity resolution.
- **Confidence check doesn't parse confidence language as standalone evidence** — it only ever cross-references a claim against the wiring verdict, by design.
- **JS `axios({method:'post', url:'/x'})` object-call style, and Flask's bare `@app.route(...)` without an explicit method, aren't parsed** — left `NOT TESTED` rather than guessed.
- **`malveon watch` doesn't work over NFS/SMB** (fsnotify's own constraint) and can fail to start on very large repos if the OS's inotify-watch/file-descriptor limits are hit — surfaced as a real error, never swallowed.

## Demo readiness (before sending to Feeling_Sun_6436)

- [x] Example repo with a wired feature, a broken one, a dynamic-path one, a method-mismatch one (`testdata/fixture`)
- [x] Sample plain-text hero-act and confidence inputs + expected results (proved manually this session)
- [x] Full expected terminal output captured from real runs, including `malveon watch` catching a real bug live
- [ ] Release binaries rebuilt with `fsnotify` (see build checklist)
- [ ] Actually pushed + released (blocked on founder go-ahead)
- [ ] Message drafted to Feeling_Sun_6436

## Open questions / blockers

Waiting on founder confirmation to push `beta-test` to the public remote and create the v0.1.0 GitHub Release.

## Log

- **2026-09-11 (planning)** — Confirmed Go, own-parser-referencing-graphify, four checks in v1, language coverage, hero-act input format, session-snapshot capture.
- **2026-09-11 (implementation, part 1)** — Pivoted from `go-tree-sitter` to pure Go (no C compiler on the build machine). Built and proved the wiring check end-to-end.
- **2026-09-11 (implementation, part 2)** — Built contract, not-in-plan, hero-act, and session lifecycle. Added `internal/gitutil` and `internal/session`. Proved the full 4-check pipeline end-to-end. Verified cross-compilation (linux/amd64, darwin/arm64, windows/amd64).
- **2026-09-16 (manual verification + icon)** — Founder independently ran the full test suite and CLI on their own machine (PowerShell), confirming every verdict matched. Embedded the Malveon logo into the Windows binary via `rsrc`, verified genuinely present and cross-platform-safe.
- **2026-09-16 (repo split + release prep)** — Found and fixed the private-repo-remote-pointed-at-public-repo leak risk. Split `beta-test/` into its own real git repo, one clean commit. Wrote `README.md`. Built 4-platform release binaries. Stopped before pushing/releasing pending explicit go-ahead.
- **2026-09-16 (three more checks)** — Founder listed 6 real pain points and asked which were built; 3 were gaps (overlap detection, any-format plan input, and no way to catch the agent's stated confidence contradicting reality). Routed through `/software` (`refactoring` for scoping overlap detection precisely enough to avoid false positives; `philosophy-of-software-design` for keeping the multi-format plan loader a clean deep module instead of a leaky one). Built and tested all three: `internal/features` now accepts JSON/Markdown/plain-text; `internal/checks/overlap` catches colliding route registrations; `internal/checks/confidence` catches the agent's own claim contradicting the wiring verdict. All proven end-to-end through the real CLI, not just unit tests.
- **2026-09-16 (frontend overlap — "overlap" clarified to mean visual UI overlap)** — Founder clarified pain point #3 meant *visual* frontend overlap (elements colliding on screen), not backend route collision. Worked through why that fundamentally needs real rendering (browser/layout engine) to confirm geometrically, which conflicts with the whole tool's no-live-testing identity. Walked through all 10 real CSS-overlap bug categories against what's actually statically knowable vs. render-dependent — the key finding: *stacking/positioning structure* is fully knowable from source, *actual geometric collision* is not. Designed a tiered plan (static structural-risk checks always-on; live confirmation kept separate, optional, and narrow) instead of an all-or-nothing choice. Founder chose "Tailwind first" over full plain-CSS cascade resolution for v1 (much smaller build, matches what most AI-agent-generated frontends actually use). Built `internal/checks/uioverlap`: flags `absolute`/`fixed` Tailwind classes with no `relative`/`sticky` context anywhere in the same file. Caught and fixed a real logic bug during testing (absolute/fixed were wrongly counted as their own "positioning context," silently canceling every finding) — found via the test suite failing, not assumed correct. Proven end-to-end. Now 7 checks total. Live-render confirmation and plain-CSS support both explicitly deferred, not built.
- **2026-09-16 (command UX — plan file auto-detection)** — Founder flagged that `malveon check --features ... --bugs-reported ... --claimed-summary ...` was too long to memorize, and proposed an interactive prompt for finding the plan file (like `AskUserQuestion`). Built `features.Detect(root)` (top-level scan for plan-looking filenames) plus `resolveFeaturesPath` in `cmd/malveon`: one candidate → auto-used and announced; multiple → asks interactively; none → asks for a path; non-interactive (scripted/CI) and ambiguous → fails immediately with a clear error instead of hanging on stdin forever. `--features` still works exactly as before for scripts. `malveon check` now runs with zero required flags in the common case. Proven end-to-end, including the non-interactive fail-fast path (verified it doesn't hang).
- **2026-09-16 (plan file auto-detection, part 2 — content fallback)** — Founder pointed out the real gap: name-based detection does nothing for a plan file with no name hint at all (`sprint3.json`, `notes.md`, anything). Built `features.DetectByContent(root)` as a fallback tier when name-matching finds nothing: `.json` files count only if they actually parse as an array of objects with a `name` field (the real expected shape, not just "valid JSON"); `.md` files count only with ≥2 real checklist-syntax lines. `.txt` deliberately excluded from this fallback — no reliable content signal without a name hint, and guessing there would break the tool's own core rule. Proven end-to-end: a plan named `sprint3.json` with zero name hints got found and used automatically, `README.md` correctly ignored.
- **2026-09-16 (hero-act automation attempt — rejected, correctly)** — Proposed sourcing hero-act's bug list automatically from commit messages (fix-keyword scan). Founder correctly rejected it: trusting a commit message's own "this is a fix" label as evidence is circular — the exact self-report this check exists to not trust, just relocated from chat into git, and gameable by simply not writing "fix" in the message. Routed through `/software` → `why-programs-fail`'s omniscient-debugging concept to find a genuinely self-report-free alternative: confirmed that fully automatic detection needs *some* record of the intermediate broken state, and without either a self-report or actual test execution, there's no way to distinguish "the agent fixed a real bug" from "the agent iterated normally while writing new code" from pure snapshots alone — an information-theoretic limit, not a design gap. Scoped the real path forward (background snapshotting + a known bad-pattern catalog now, test-suite pass/fail signal later) but did not build it this session — documented as deferred in `CLAUDE.md` section 4, hero-act stays on manual `--bugs-reported`.
- **2026-09-16 (confidence check automated)** — Same request applied to the confidence check, but this time it holds up: unlike hero-act, confidence-check doesn't need the commit message to be honestly self-labeled as anything — it only captures whatever confident language the agent actually wrote, then checks that claim against a verdict computed entirely independently (wiring). An agent writing modest commit messages just produces more `NOT TESTED`, not an evasion. Built `gitutil.CommitMessagesSince` and rewired `confidence.Run` to read from it automatically when `--claimed-summary` isn't given (requires a session to have been started). Proven end-to-end with real git commits and zero flags: a commit message claiming "Cancel order is done and working perfectly, ready to ship" was read automatically and correctly flagged as a CONFIDENCE MISMATCH against the real (failing) wiring result.
- **2026-09-16 (hero-act automation, real version — `malveon watch`)** — Founder pushed back again: didn't want the "find it from code, don't depend on commits" idea dropped just because a single before/after diff can't see an intermediate state. Explained the actual information-theoretic wall with a room/table analogy (you can't tell a messy table got cleaned if you weren't watching), then reframed correctly: the fix isn't smarter analysis of the final diff, it's a real "camera" — a tool-owned process, independent of git and independent of any self-report. Routed through `/software` (`evolutionary-architectures` for cadence — confirmed **continual**, event-driven capture is the only cadence without a structural blind spot, not triggered-only or fixed-interval) plus a live web search (fsnotify confirmed as the standard, actively-maintained, pure-Go cross-platform watcher — first external dependency this project has taken on, verified it doesn't reintroduce cgo or break cross-compilation). Built: `internal/watch` (fsnotify-based recursive watcher, manual recursive-add and Chmod-filtering per the search results, debounced snapshots to `.malveon/history/`, its own liveness heartbeat so a crashed watcher can't silently masquerade as complete history) and `internal/checks/heropatterns` (a 3-pattern known-bug catalog — JS/TS assignment-in-condition, Go empty-error-handling, Python bare-except — flagging a pattern present in an earlier snapshot and gone from the current file). Wired as `malveon watch` (new CLI command) and a new automatic hero-act section in `check`, running alongside the existing manual `--bugs-reported` path rather than replacing it. Proven fully end-to-end through the real CLI: started `malveon watch` as a real background process, wrote a Go file with a real empty-error-handling bug, "fixed" it seconds later, and `malveon check` — while the watcher was still live — correctly flagged it automatically, zero commits, zero self-report. (Hit and worked around a Windows-specific test-environment snag: git-bash's `kill -INT` doesn't reliably deliver a Windows console Ctrl+C to a separate process, so the demo script hung waiting for graceful shutdown — resolved with `taskkill`; not a bug in the watcher itself, which already had a clean `context.Done()` shutdown path proven by its own Go test.)
- **2026-09-16 (manual hero-act removed)** — With `malveon watch` + `heropatterns` proven as a genuine, self-report-free replacement, founder quoted the comparison table row showing the old manual path was "still there" and said to remove it. Interpreted as removing it from the product, not just the table, consistent with the session's pattern of retiring self-report mechanisms once a real alternative exists. Deleted `internal/checks/heroact/` outright (both the package and its test), removed the `--bugs-reported` flag and all call sites from `cmd/malveon/main.go` and `internal/report/report.go`. `go vet`, `go build`, and the full `go test ./...` suite all passed clean immediately after.
- **2026-09-16 (confidence check challenged again, mid-turn — resolved with `incompleteness`)** — Founder interrupted mid-turn, quoting a line from this session's own "fully automatic, reads commit messages" explanation, insisting the same "we're not magicians reading commit messages, we should read from the code" standard that produced `malveon watch` also applies to confidence-check. Rather than rip out `confidence.go`'s commit-message sourcing, reasoned through why the two checks are structurally different: hero-act's question ("did a known bad pattern appear then disappear") is a fact about code structure over time, answerable from snapshots alone; confidence-check's question ("did the agent claim X works") is inherently linguistic — there's no code-structural signal for "a claim was made," only for whether the claim's underlying assertion is true, which is exactly what the wiring cross-reference already checks. Validated via `/software` (router found no new skill match — confirmed via its own "don't overuse" clause that general judgment already covered this) and a live web search ("static analysis detect incomplete AI generated code without running tests") — confirmed real tools (SonarQube etc.) only ever detect specific named patterns, never general "completeness," and that TODO/FIXME/HACK markers are an established, legitimate, code-only static-analysis signal, separate from confidence-checking. Landed on building a new, additional, narrower check instead of altering `confidence.go`. Built `internal/checks/incompleteness` (scans files changed since session start for `TODO`/`FIXME`/`HACK`/`XXX`/"not implemented" markers across `.js/.jsx/.ts/.tsx/.py/.go`, session-scoped via the same `session.Load`/`gitutil.ChangedFilesSince` pattern as `planauthority`), its test suite (marker in a session-changed file flagged, marker predating the session correctly excluded, unavailable without a session), wired it into `cmd/malveon/main.go` and `internal/report.WriteIncompleteness`. Explicitly documented as one-directional — presence is proof, absence isn't, never a substitute for confidence-check. Proven end-to-end through the real built CLI: a temp repo with a `TODO` and a `FIXME` added after `session start` correctly flagged both with file:line and the matching text; a marker predating the session correctly excluded. Full suite (`go vet`, `go build`, `go test ./...`) passed clean. Now 9 checks total.
- **2026-09-16 (five real gaps closed)** — Founder asked what was actually missed this session and what bugs were self-introduced-and-caught. Answered honestly with a 7-item gap list, then asked to fix them via `/software`. Router + own reasoning split the list: 4 items (contract body-shape, hero-act's general judgment, plain-CSS overlap, incompleteness's general case) were already deliberately deferred decisions from earlier in the session, not oversights — fixing those silently would mean reopening decisions without asking. Surfaced this distinction to the founder via `AskUserQuestion` before touching anything. Founder chose to build the fixable ones (#1 wiring reason text, #4 overlap reachability, #7 Python/Go fixture parity) plus, after a second question about whether to break the static-only rule for hero-act/incompleteness's *general* case, explicitly chose to keep static-only and build #2 (contract body-shape) and #5 (plain-CSS overlap) as bigger *scoped* static-analysis work instead — confirming #3/#6's general "was this really a bug/is this really wrong" questions stay permanently deferred (a real limit, not a TODO: can't know without either running the code or trusting a self-report).
  - **#1 — wiring's vague `NOT TESTED` reason.** Now says exactly which side is missing (`internal/checks/wiring/wiring.go`'s `missingSideReason`). Small, contained, tested with 3 hand-built-graph cases.
  - **#4 — overlap reachability.** Investigated mid-build and found the real cost: JS/Python extractors had zero concept of function scope (Go's AST gets it free). Flagged this to the founder before building a noisy heuristic (e.g. indentation-based) that would've violated the tool's own "never guess" rule. Built the real version instead: `funcRange`/`enclosingFuncFor` shared machinery across all 3 extractors (brace-matching for JS/TS, indentation-walking for Python, `go/ast` for Go), plus `internal/checks/overlap/reachability.go`'s word-count reference check. Proven end-to-end: a route inside a function referenced nowhere else got flagged "possibly unreachable"; a top-level route didn't.
  - **#7 — Python/Go fixture parity.** Built `testdata/fixture-python` and `testdata/fixture-go`, same PASS/FAIL/NOT-TESTED/method-mismatch shape as the JS/TS fixture. Caught a real fixture-design bug immediately: `@app.route(..., methods=["POST"])` doesn't resolve the method in this extractor (a real, already-documented v1 limitation) — first draft used that syntax and got NOT TESTED everywhere; fixed by switching to `@app.post(...)`-style decorators, which do resolve.
  - **#2 — contract body-shape.** Built the honestly-scoped slice: JS/TS only, does the call send every field the route handler reads off `req.body`, only when both sides resolve to a plain literal object. New `internal/extractor/bodyshape.go` (object-literal key extraction, balanced-bracket reading, JSON.stringify/axios/direct-object body patterns, `req.body.x`/`req.body['x']`/destructure read-field extraction). A spread anywhere marks the whole object unresolved rather than undercounting — the same "never overclaim" discipline applied to a brand-new extraction path. `contract.Result` got a second, independent `BodyVerdict`/`BodyReason`, reported as its own `[fields: ...]` line under the existing `[method: ...]` line, never folded together. Proven end-to-end: a call missing a field the handler reads got a real CONTRACT MISMATCH naming the missing field.
  - **#5 — plain-CSS overlap.** Extended `internal/checks/uioverlap` to `.css`/`.scss`/`.less`, scanning for `position:` declarations instead of Tailwind class names. Deliberately kept context detection as a flat whole-file text search rather than brace-pairing each declaration to its selector — proven necessary by a dedicated test (`TestRunPlainCSSNestedContextNotMissed`): a parent selector's `relative` nested around a child's `absolute` would be missed by a brace-matched scan, producing a false positive a flat search doesn't have.
  - All 5 fixes proven via unit tests, the full suite (`go vet`, `go build`, `go test -count=1 ./...`), and a real end-to-end CLI demo each. Docs (`CLAUDE.md`, `README.md`, this file) updated for all 5. Release binaries not yet rebuilt for this batch.
- **2026-09-16 (hero-act scope corrected — "created," not "created and fixed")** — While walking through the 6 original pain points from the user's point of view, founder corrected the hero-act description directly: "we're not here to check the bugs created and fixed, we're here to find the bugs where created in the session/chat." The shipped check only ever looked at the "introduced-then-fixed" half (a pattern present in an earlier watch snapshot, gone now) — a bug introduced this session and still sitting there, unfixed, was invisible to it. Realized the "still present" half doesn't need `malveon watch` at all: it's a plain two-point comparison between the file's content at session start (git) and its current version, the same mechanism `incompleteness`/`planauthority` already use. Routed through `/software` → `evolutionary-architectures` to confirm the shape: git-baseline (triggered, always-available) and watch-history (continual, richer-when-present) protect the exact same fitness-function concern and belong in one check with two sub-labels, not two separate checks. Built `gitutil.FileAtRef` (`git show ref:path`, treats "doesn't exist at that ref" as a clean non-error result since a brand-new file has no baseline to compare against) and rewired `heropatterns.Run` to merge both signals: `INTRODUCED THIS SESSION — STILL PRESENT` (git-only, always available once a session started) and `INTRODUCED & FIXED SAME SESSION` (the original watch-based signal, renamed). Report now notes plainly when watch wasn't running, since that specifically disables the "fixed" half while the "still present" half keeps working. Proven end-to-end: a real Go file with an empty-error-handling bug, introduced after `session start` and never fixed, correctly flagged with zero `malveon watch` involved; a pre-existing bug in a file touched again for something unrelated correctly stayed unflagged. Full suite passed clean.
- **2026-09-16 (install friction removed)** — Founder asked to remove the manual download/chmod/PATH/Gatekeeper steps from the install walkthrough. Built `install.sh` (macOS/Linux: detects OS/arch, downloads the matching release asset, `chmod +x`, places it on PATH, clears macOS's quarantine flag so Gatekeeper doesn't block the first run) and `install.ps1` (Windows: downloads the exe, adds its folder to the user's PATH, no admin needed). Both run via a single `curl | sh` / `irm | iex` one-liner. README's Install section now leads with these, keeping the manual steps as a documented fallback. Syntax-checked `install.sh` (`sh -n`) and verified its OS/arch detection logic directly; couldn't test an actual download since no GitHub Release exists yet (still gated on founder go-ahead) — both scripts are ready for whenever that happens.
- **2026-09-16 (first real external test — a real bug in the plan-file detector, found and fixed)** — Founder ran `malveon` against a real project ("5PM Club" Next.js app) via Antigravity for the first genuine end-to-end field test. It silently picked `README.md` as the plan file instead of the project's actual `docs/PLAN.md`, then founder said directly: "our the tool needs to ask to select which is the plan file." Root cause, confirmed by checking the real repo: `features.Detect`/`DetectByContent` were top-level-only (deliberately, from the original 2026-09-16 design) — `docs/PLAN.md` was never even scanned, so name-based detection found nothing, fell to the weaker content-based tier, and README.md's checklist-looking lines won by default as the *only* candidate, which the existing "exactly one candidate → auto-use" rule then trusted silently. A confidently-wrong result from the tool's own plan detection — the exact failure mode this whole tool exists to catch in *other* people's code, now caught happening to itself, by the real external tester it was built for. Fixed two things: (1) made `Detect`/`DetectByContent` recurse the whole tree (skipping the same noise dirs the extractor already does, `.next` added new after seeing a real Next.js project), so a deliberately-named `docs/PLAN.md` is now actually found; (2) changed `resolveFeaturesPath` so a content-based single match is *never* auto-used the way a name match is — it's shown and confirmed (`found a possible plan file by content, not by name: X — use it? [Y/n]`) before use, matching exactly what the founder asked for. Caught and fixed a second, real bug while building this: `readLine` created a fresh `bufio.Scanner` per call, so the new two-read confirm-then-type-a-path flow silently lost buffered input on the second read — found via a failing test, fixed by sharing one scanner across a whole `resolveFeaturesPath` call. Proven end-to-end against a reconstruction of the exact reported scenario (`docs/PLAN.md` + a checklist-flavored `README.md`): name-based detection now finds `docs/PLAN.md` directly, and a content-only match now requires confirmation instead of silently winning. Full suite passed clean; release binaries rebuilt.
- **2026-09-16 (plan-file auto-use removed entirely — "there shouldn't be any assumptions anymore")** — Founder's very next reaction to the fix above: "no directly asks to which file is plan file! there shouldn't be any assumptions anymore." The just-shipped fix still auto-used a single *name*-matched candidate (e.g. a literal `PLAN.md`) without asking, on the reasoning that a deliberate name match was a strong enough signal — founder rejected that exception too. Removed it: `resolveFeaturesPath`'s single-candidate branch no longer distinguishes name vs. content confidence in *behavior*, only in the message shown (`found a possible plan file (by name): X` vs `(by content, not by name): X`) — both now require an explicit `use it? [Y/n]` before anything reads from the file, and saying no prompts for the real path directly. The only way to skip being asked at all is passing `--features` explicitly. Rewrote `prompt_test.go`'s single-candidate tests to assert confirmation is always required, added a name-match-rejected-falls-back-to-manual-path test. Proven end-to-end: a real `PLAN.md` (unambiguous by name) now still requires a `[Y/n]` answer before use, in both the accept and reject direction. Full suite passed clean; release binaries rebuilt.
- **2026-09-16 (Next.js App Router routes — real 0-PASS result explained and fixed)** — With plan detection now correctly finding `docs/PLAN.md`, the real test run against the 5PM Club project came back `0 PASS, 43 NOT TESTED` — every single feature. Investigated by reading a real route file (`src/app/api/jobs/route.ts`) instead of assuming the plan or the project was at fault: found `export async function GET(request: Request) {...}`, Next.js App Router's route-handler convention — the HTTP method is the *exported function's name*, and the URL path comes from the *file's location on disk*, nothing like the `app.get(path, handler)` call the extractor's `jsRoutePattern` looks for. The project has 64+ genuinely built API routes under this convention; every one of them was invisible to the tool. Built `internal/extractor/nextjs.go`: `isNextRouteFile` matches Next.js's reserved `route.ts`/`.tsx`/`.js`/`.jsx` filename; `scanNextRouteHandlers` extracts one RouteHandler per exported `GET`/`POST`/`PUT`/`DELETE`/`PATCH`/`HEAD`/`OPTIONS` (both `export function X` and `export const X =` styles); `nextRoutePath` derives the URL from the file's path relative to the nearest `app`/`src/app` ancestor, stripping route-group folders (`(app)`) and normalizing `[id]`/`[...slug]`/`[[...slug]]` to the `:id` wildcard syntax `graph.PathsMatch` already understands — zero changes needed anywhere else in the pipeline. Tested all three dynamic-segment forms, route groups, both export styles, and a real end-to-end extractor test (route file + matching fetch call → both nodes found, wired correctly). Proven against the actual project that found the gap: `0 PASS, 43 NOT TESTED` → `10 PASS, 0 FAIL, 33 NOT TESTED` on the identical plan file, with real evidence lines connecting frontend calls to their Next.js route handlers. Stated limitation: App Router only, the older Pages Router convention isn't covered. Full suite passed clean; release binaries rebuilt.
- **2026-09-16 (reserved-filename matching bug — "fix the all one by one")** — Founder asked, via `/software`, to fix the remaining NOT TESTED causes one by one, after I explained what each of the 4 reason categories meant. Clarified scope first (a genuine ambiguity worth asking about): fix malveon's own detection, not go implement the missing features in the real 5PM Club app — confirmed via `AskUserQuestion`. Investigated the remaining 33 NOT TESTED entries by counting evidence lines per entry rather than assuming: one outlier stood out immediately, 75 evidence lines for "Build `/circles` directory page..." versus 2–6 for everything else. Root cause: the feature description's ordinary word "page" was matching every file literally named `page.tsx` — Next.js App Router's reserved convention for a route segment's UI, which makes that filename appear almost everywhere in the app. `wordsFor`'s loose word-overlap matcher had no concept of "this filename is structurally reserved, not descriptive." Asked the founder how wide the fix should go (Next.js-only vs. also covering other languages' near-universal filenames) — founder chose the wider scope. Added `reservedBaseNames` to `internal/extractor/extractor.go`: Next.js's `page`/`layout`/`loading`/`error`/`template`/`default`/`not-found`/`route`/`global-error`/`middleware`, plus `index` (JS/TS), `main` (Go), `__init__` (Python) — `wordsFor` now skips contributing a file's own name as a match word when it's one of these, so a node with no other real signal becomes correctly un-matchable instead of riding along on filename coincidence. Checked the rest of the remaining 33 NOT TESTED entries for the same pattern first (small, reasonable evidence counts — genuine results, not more of this bug) before concluding this was the one fix needed. Proven against the real project: the Circles feature's evidence dropped from 75 irrelevant lines to a small, precise, genuine "frontend call found, but no matching backend route." Full suite passed clean; release binaries rebuilt.
